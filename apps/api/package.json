{
  "name": "@tigerhacks25/api",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": ".venv/bin/python -m uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "start": ".venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "install:python": ".venv/bin/pip install -e .",
    "migrate": ".venv/bin/python run_migration.py"
  }
}
